<form method="post" action="{{ path('app_c_p_participation_delete', {'id': c_p_participation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ c_p_participation.id) }}">
    <button class="btn">Delete</button>
</form>
