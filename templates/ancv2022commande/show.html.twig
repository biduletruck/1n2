{% extends 'base.html.twig' %}

{% block title %}Ancv2022commande{% endblock %}

{% block body %}
    <h1>Ancv2022commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ancv2022commande.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ ancv2022commande.CreatedAt ? ancv2022commande.CreatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ancv2022commande_index') }}">back to list</a>

    <a href="{{ path('app_ancv2022commande_edit', {'id': ancv2022commande.id}) }}">edit</a>

    {{ include('ancv2022commande/_delete_form.html.twig') }}
{% endblock %}
