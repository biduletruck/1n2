<form method="post" action="{{ path('package21_delete', {'id': package21.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ package21.id) }}">
    <button class="btn">Delete</button>
</form>
