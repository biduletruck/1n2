{% extends 'base.html.twig' %}

{% block title %}Edit Participations{% endblock %}

{% block body %}
    <h1>Edit Participations</h1>

    <a href="{{ path('participations_index') }}">back to list</a>
    {% if is_granted("ROLE_SUPERADMIN") %}
        {{ include('participations/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('participations/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
