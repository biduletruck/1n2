{% extends 'base_noel.html.twig' %}

{% block title %}Cheques{% endblock %}

{% block body %}
    <h1>Cheques</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cheque.id }}</td>
            </tr>
            <tr>
                <th>NomCheque</th>
                <td>{{ cheque.nomCheque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cheques_index') }}">back to list</a>

    <a href="{{ path('cheques_edit', {'id': cheque.id}) }}">edit</a>

    {{ include('cheques/_delete_form.html.twig') }}
{% endblock %}
