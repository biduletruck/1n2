{% extends 'base.html.twig' %}

{% block title %}Commande21{% endblock %}

{% block body %}
    <h1>Commande21</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande21.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ commande21.createdAt ? commande21.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EmailSalarie</th>
                <td>{{ commande21.emailSalarie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande21_index') }}">back to list</a>

    <a href="{{ path('commande21_edit', {'id': commande21.id}) }}">edit</a>

    {{ include('commande21/_delete_form.html.twig') }}
{% endblock %}
