<form method="post" action="{{ path('commande21_delete', {'id': commande21.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande21.id) }}">
    <button class="btn">Delete</button>
</form>
