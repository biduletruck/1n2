<form method="post" action="{{ path('bbq_event_delete', {'id': bbq_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bbq_event.id) }}">
    <button class="btn">Delete</button>
</form>
