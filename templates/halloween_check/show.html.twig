{% extends 'base.html.twig' %}

{% block title %}HalloweenCheck{% endblock %}

{% block body %}
    <h1>HalloweenCheck</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ halloween_check.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ halloween_check.createdAt ? halloween_check.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('halloween_check_index') }}">back to list</a>

    <a href="{{ path('halloween_check_edit', {'id': halloween_check.id}) }}">edit</a>

    {{ include('halloween_check/_delete_form.html.twig') }}
{% endblock %}
